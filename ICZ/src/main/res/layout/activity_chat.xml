<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <!-- The PullToRefreshScrollView replaces a standard PullToRefreshScrollView widget. -->
    <com.handmark.pulltorefresh.library.PullToRefreshListView
        xmlns:ptr="http://schemas.android.com/apk/res-auto"
        android:id="@+id/chat_msg_list_view"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        ptr:ptrAnimationStyle="flip"
        android:layout_alignParentTop="true"
        android:layout_marginBottom="66dp"
        ptr:ptrMode="both" />

    <RelativeLayout
        android:id="@+id/chat_btm_con"
        style="@style/SendTextEditBtmConStyle">

        <EditText
            android:id="@+id/chat_edit_text"
            style="@style/SendTextEditStyle"
            android:inputType="text"/>

        <Button
            android:id="@+id/chat_send"
            style="@style/SendTextBtnStyle"/>
    </RelativeLayout>

</RelativeLayout>